# Zensical (replaces mkdocs + mkdocs-material)
zensical

# Plugins
mkdocstrings[python]>=0.24.0

# Additional dependencies
pymdown-extensions>=10.0.0

# Required for mkdocstrings to collect API docs (install from master for unreleased classes)
toolregistry-hub @ git+https://github.com/Oaklight/toolregistry-hub.git@master
