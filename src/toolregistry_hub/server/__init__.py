"""ToolRegistry Hub Server module.

Provides Hub-specific server components:
- ``registry``: Tool registry construction with Hub defaults
- ``cli``: CLI entry point with Hub banner and options

For server infrastructure (adapters, routing, auth), import
directly from ``toolregistry_server``.
"""
